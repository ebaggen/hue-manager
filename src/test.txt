import csv
import math

class ColorGenerator:
    
    def getXY(temperature):
        # round temperature to nearest value in lookup table
        rounded_temperature = min(cct_lookup, key=lambda x:abs(float(x) - temperature))
        
        return cct_lookup[rounded_temperature]
    

cct_lookup = {}
with open ('CCT-lookup-table.txt', mode='r') as file:
    reader = csv.DictReader(file, delimiter='\t')
    for entry in reader:
        cct_lookup[entry['CCT']] = [entry['x (black body)'],
                                          entry['y (black body)']]

temperature = 1236
a = 
print(a)